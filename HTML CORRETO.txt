<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Assistente GPT</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f0f0f0;
      padding: 20px;
      max-width: 800px;
      margin: auto;
    }
    #chat {
      background: #fff;
      padding: 15px;
      border: 1px solid #ccc;
      height: 400px;
      overflow-y: auto;
      margin-bottom: 15px;
    }
    input[type="text"] {
      width: 75%;
      padding: 10px;
      font-size: 16px;
    }
    button {
      padding: 10px 20px;
      font-size: 16px;
    }
    .message {
      margin: 10px 0;
    }
  </style>
</head>
<body>
  <h2>Converse com o Assistente GPT 🤖</h2>
  <div id="chat"></div>
  <input type="text" id="userInput" placeholder="Digite sua pergunta..." />
  <button onclick="sendMessage()">Enviar</button>

  <script>
    async function sendMessage() {
      const input = document.getElementById("userInput");
      const question = input.value.trim();
      if (!question) return;

      const chat = document.getElementById("chat");
      chat.innerHTML += `<div class="message"><strong>Você:</strong> ${question}</div>`;

      // 🔁 Substitua pela URL da API do seu agente GPT personalizado
      const response = await fetch("https://SEU-LINK-DO-AGENTE", {
        method: "POST",
        headers: {
          "Content-Type": "application/json"
        },
        body: JSON.stringify({ message: question })
      });

      const data = await response.json();
      chat.innerHTML += `<div class="message"><strong>Assistente:</strong> ${data.reply || 'Não consegui entender.'}</div>`;
      input.value = "";
      chat.scrollTop = chat.scrollHeight;
    }
  </script>
</body>
</html>
